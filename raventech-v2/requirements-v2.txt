# Phase 1 v2 dependencies.
# Heavy ML libs (torch, faiss) still deferred to Phase 2.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
gunicorn>=22.0,<24.0
pydantic>=2.7,<3.0
python-dotenv>=1.0,<2.0
redis>=5.0,<6.0
httpx>=0.27,<0.29

# Phase 1 module 4 — agent committee. Claude SDK pulls in pinned
# versions of httpx/anyio that are compatible with the FastAPI stack.
anthropic>=0.39,<1.0
